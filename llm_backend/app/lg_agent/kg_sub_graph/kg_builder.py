"""占位文件（原上游示例的独立图谱构建入口）。

当前项目的图谱构建与连接逻辑位于：
- `app/lg_agent/kg_sub_graph/kg_neo4j_conn.py` —— Neo4j 连接
- `app/lg_agent/kg_sub_graph/agentic_rag_agents/ingest/` —— 示例语料导入
- `app/graphrag/origin_data/create_neo4j_import.py` —— 电商图谱数据导入

本文件不被任何模块引用，保留仅为与上游目录结构保持一致。
"""
