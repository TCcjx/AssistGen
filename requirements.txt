# 仅支持 Python 3.11 ~ 3.12：graphrag==2.1.0 声明 python>=3.10,<3.13，
# 而 ipython==9.1.0 声明 python>=3.11，两者叠加后 3.10 与 3.13+ 都会安装失败。
fastapi>=0.68.0
uvicorn>=0.15.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
# 以下四项虽由 fastapi / pydantic / graphrag 传递安装，但项目代码直接 import，
# 按「直接依赖必须显式声明」的原则列出，避免上游版本变动后启动即失败
starlette>=0.27.0
pydantic-core>=2.0.0
typing-extensions>=4.7.0
aiofiles>=23.0.0
openai>=1.0.0
httpx>=0.24.0
python-dotenv>=0.19.0
tenacity>=8.0.0
requests>=2.28.0
aiohttp>=3.8.0
sentence_transformers
faiss-cpu
PyPDF2
python-docx
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
regex>=2023.0.0
PyYAML>=6.0
tqdm>=4.65.0
python-multipart>=0.0.5
loguru==0.7.2
sqlalchemy[asyncio]>=2.0.0
aiomysql>=0.1.1
cryptography  
python-jose[cryptography]>=3.3.0
passlib==1.7.4
bcrypt==4.0.1
email-validator>=1.1.3
psutil>=7.0.0
GPUtil>=1.4.0
redis>=5.0.0
langchain-core>=0.3.0,<0.4.0
# multi_tools.py 与 summarize/correction/validate_final_answer 的 prompts.py
# 都使用 `from langchain.prompts import ChatPromptTemplate`，这需要 langchain 元包；
# langchain-core / langchain-community 都不会传递安装它，缺失会导致启动即 ImportError。
langchain>=0.3.0,<0.4.0
langgraph==0.3.25
langchain-deepseek==0.1.3
langchain-ollama==0.3.0
langchain_neo4j==0.4.0
# Neo4j 驱动。虽然由 langchain_neo4j 传递安装，但 app/test/test.py 等直接 import，
# 显式声明避免上游变更后启动即失败。
neo4j>=5.0.0
matplotlib==3.10.1
seaborn==0.13.2
langchain_community==0.3.21
graphrag==2.1.0
langgraph-checkpoint-sqlite==2.0.6
ipython==9.1.0

# 测试（python tests/test_contract.py 无需安装，装 pytest 后可用 pytest 方式运行）
pytest>=8.0.0

# 可选依赖，仅内嵌的第三方示例代码会用到，主链路不需要：
#   neo4j-graphrag   -> 向量化 Cypher 示例检索器（retrievers/.../vector_store）
#   streamlit        -> agentic_rag_agents/ui 演示界面
#   langchain_openai -> agentic_rag_agents/agent.py 示例脚本
